import stuff


class Bot:

    def __init__(self, max_depth, num_children):
        self.start_tree = Tree()
        self.end_tree = Tree()
        self.max_depth = ()
        self.num_children = ()

        return

    def search(self, start_hashtag, end_hashtag):
        """
        Perform graph search from start hashtag to end hashtag

        Inputs:
            start_hashtag:
            end_hashtag:

        Outputs:
            steps: List of hashtag strings from start to finish
        """
        return steps
